{
  "_comment": "OpenClaw exposes MCP tools as `<serverKey>__<toolName>`, and the pipeline skills were authored against Claude Code's `mcp__blender__*` names. Keying the server `mcp__blender` makes both harnesses produce the same tool names, so one copy of the skills runs in either.",
  "mcp": {
    "servers": {
      "mcp__blender": {
        "command": "/opt/venv/bin/blender-mcp",
        "args": [],
        "env": {
          "BLENDER_HOST": "127.0.0.1",
          "BLENDER_PORT": "9876"
        }
      }
    }
  },
  "skills": {
    "load": {
      "extraDirs": [
        "/opt/mrs/.claude/skills",
        "/opt/mrs/deploy/skills"
      ],
      "watch": false
    }
  },
  "tools": {
    "profile": "coding",
    "exec": {
      "host": "gateway",
      "security": "full",
      "ask": "off"
    }
  }
}
